npm run upload-payroll:ecampus


Test case 4 - Upload Payroll
Create a Playwright TypeScript automation script that logs into https://forms.ecampusbuddy.com/index.php/m-welcome and performs a full upload â batch â transaction reconciliation workflow.
Setup

Navigate to https://forms.ecampusbuddy.com/index.php/m-welcome.
Enter the username into the username/email field.
Enter the password into the password field.
Read credentials from environment variables (ECAMPUS_USERNAME, ECAMPUS_PASSWORD) via a .env file and the dotenv package â do not hardcode credentials in the script.

Login / Captcha

The site requires manual captcha verification. After filling in the credentials:

Pause the script and print a message asking me to solve the captcha manually in the browser window.
Wait for me to press Enter in the terminal (using Node's readline) before continuing.
Add a comment noting page.pause() as an alternative approach using the Playwright Inspector.


After I confirm the captcha is solved, click the Sign In button.
Run the browser in headed mode (headless: false) so the captcha is visible and solvable.
Use resilient selectors as a starting point (e.g., input[type="email"], input[type="password"], button:has-text("Sign In")), and add a comment noting that I should inspect the real page and adjust selectors if needed.

Dashboard â Upload

After sign-in, wait for the URL to become https://forms.ecampusbuddy.com/index.php/m-dashboard.
Click the "Upload" module link/button in the left-side navigation.
Wait for the URL to become https://forms.ecampusbuddy.com/index.php/m-upload.
Click Upload Payoll Near Upload Trial Balance.
On that page, click "Choose File" and upload the file at:
C:\Users\akspa\OneDrive\Desktop\Automation\MIS-Stagging\upload_files\PAYROOL_1.csv
Click the "Upload" button to submit.
Track the file upload time and calculate how long it takes to complete the upload. If the upload time exceeds 1 minutes, create a file named Report.payroll.env and write the following alert message into it: "Uploading payroll time is taking more than 1 minutes." Do not stop or fail the automation after logging the alert. Continue the remaining execution steps.
Batches

wait for URL to become https://forms.ecampusbuddy.com/index.php/m-batches.
click Payroll near VCC.
Once the Transaction Bathces is visible, verify the Created At date for the uploaded data. The Created At date should match today's date. If it does not match, write the following message to the Report_tb.env file: "Created At date does not match today's date." Do not stop the automation. Log the message and continue executing the remaining test steps
Verify that the total row count in the uploaded input file (sample_payroll) matches the total row count displayed in Transaction Batches. The row counts from both sources should be equal. If the counts do not match, log an alert message in the report file and continue the automation execution.
Click the RLC button. Once the file is downloaded, click the RCC button.
Compare the header rows of the input file, RLC file, and RCC file to verify whether they are identical. If the headers do not match, log the following alert message in the Report_payroll.env file:"Payroll File is not matching." Do not stop the automation after logging the alert; continue with the remaining execution steps.
